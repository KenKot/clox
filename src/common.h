#ifndef clox_common_h // Header guard - prevents multiple includes
#define clox_common_h

#include <stdbool.h> // 1, 0
#include <stddef.h> // for stuff like size_t, NULL
#include <stdint.h> // i8, u32, etc.

#endif
